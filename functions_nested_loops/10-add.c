#include "main.h"
/**
 * add - adds two numbers together
 * @i: first input
 * @j: second input
 * Return: returns the sum of the numbers
 */
int add(int i, int j)
{
	int s;

	s = i + j;
	return (s);
}
