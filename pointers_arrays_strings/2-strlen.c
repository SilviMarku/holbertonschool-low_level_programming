#include "main.h"

/**
 * main - Write a function that returns the length of a string
 *
 * @n: pointer to the variable to apdate
 */

int _strlen(char *s)
{
    int l = 0;
    while(s[i] != '\0')
	    l++;
    return (l);
}	
