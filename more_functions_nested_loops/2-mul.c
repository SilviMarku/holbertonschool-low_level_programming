#include <unistd.h>
#include "main.h"

/**
 * mul - main function
 * @a: first parameter
 * @b: second parameter
 * Return: 1 if c is a digit and 0 if otherwise
 */

int mul(int a, int b)
{
	return (a * b);
}
